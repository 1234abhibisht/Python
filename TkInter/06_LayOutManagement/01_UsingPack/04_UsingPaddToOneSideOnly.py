# as padding add extra pixels to both left and right side
# similarly to both up and down

# but if we want to add extra pixels to only left side or only upper side
# we can do it by using tuple (10, 0)
# so now, 10 pixels will be added to left side only, not right side


# padx = (10, 0) -> pixels will be added to left only
# padx = (0 , 10) -> pixels will be added to right only

# pady = (10, 0) -> pixels will be added to up only
# pady = (0 , 10) -> pixels will be added to down only 