from tkinter import *
root = Tk()
label_frame = LabelFrame(root, text = "Hello", font = ("Aerial", 30))
label_frame.place(x = 600, y = 400, height = 300, width = 500)
root.mainloop()

# it is compulsory to give height and width to add label frame